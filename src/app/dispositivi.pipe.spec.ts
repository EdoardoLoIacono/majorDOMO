import { DispositiviPipe } from './dispositivi.pipe';

describe('DispositiviPipe', () => {
  it('create an instance', () => {
    const pipe = new DispositiviPipe();
    expect(pipe).toBeTruthy();
  });
});
